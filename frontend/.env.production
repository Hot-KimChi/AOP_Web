# .env.production
#
# 기본값 없음 — src/lib/apiBase.js 가 접속한 호스트에서 API 주소를 자동 산출한다.
# (예전에는 서버 IP 를 하드코딩해 두어 서버 IP 가 바뀌면 전체가 깨졌다.)
#
# 백엔드가 프론트와 다른 호스트/포트에 있을 때만 아래를 지정한다.
# NEXT_PUBLIC_API_BASE_URL=http://<백엔드호스트>:5000
